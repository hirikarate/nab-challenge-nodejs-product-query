export enum AWS {
	REGION = 'aws_region_code',
	ACCESS_KEY = 'aws_access_key',
	SECRET_KEY = 'aws_secret_key',
}

export enum ElasticSearch {
	HOST = 'elastic_search_host',
	MAPPINGS = 'elastic_search_mappings',
}
